# 🚀 ВК Реклама - Букмарклет версия

Веб-сервис для массового дублирования рекламных групп ВКонтакте через букмарклет (кнопка в закладках).

## ✨ Особенности:

- ⚡ **Работает без установки расширений!**
- 🎨 Интерфейс появляется поверх ads.vk.com
- 🔐 Все данные остаются в браузере пользователя
- 📱 Адаптивный дизайн
- 🚀 Быстрое дублирование (5 сек на группу)

---

## 📦 Установка на сервер

### 1. Требования:
- Ubuntu 20.04+ / Debian 11+
- Node.js 18+
- Nginx
- Доменное имя (опционально)

### 2. Подключаемся к серверу:
```bash
ssh root@ваш_ip
```

### 3. Устанавливаем Node.js:
```bash
apt update && apt upgrade -y
curl -fsSL https://deb.nodesource.com/setup_20.x | bash -
apt install -y nodejs
node --version
```

### 4. Загружаем проект:
```bash
cd /var/www
# Загрузите файлы проекта сюда
```

### 5. Устанавливаем зависимости:
```bash
cd /var/www/vk-ads-server
npm install
```

### 6. Настраиваем домен:

**ВАЖНО!** Замените `https://ваш-домен.ru` в двух файлах:

1. `public/index.html` (строка с href="javascript:...)
2. `public/bookmarklet.js` (строка const API_BASE)

На ваш реальный домен!

### 7. Запускаем с PM2:
```bash
npm install -g pm2
pm2 start server.js --name vk-ads-scaler
pm2 startup
pm2 save
pm2 logs
```

### 8. Настраиваем Nginx:
```bash
nano /etc/nginx/sites-available/vk-ads-scaler
```

Вставьте:
```nginx
server {
    listen 80;
    server_name ваш-домен.ru;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

Активируем:
```bash
ln -s /etc/nginx/sites-available/vk-ads-scaler /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### 9. Устанавливаем SSL:
```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d ваш-домен.ru
```

---

## 🎯 Как пользователи используют:

1. Открывают ваш сайт: `https://ваш-домен.ru`
2. Перетаскивают кнопку "🚀 ВК Масштабер" в закладки
3. Идут на ads.vk.com
4. Кликают закладку → появляется интерфейс!
5. Вводят токен, ID группы, количество → дублируют!

---

## 📁 Структура проекта:

```
vk-ads-server/
├── server.js              # Backend прокси для VK API
├── package.json           # Зависимости
├── .env                   # Конфигурация
└── public/
    ├── index.html         # Главная страница с инструкциями
    └── bookmarklet.js     # Скрипт букмарклета (загружается поверх страницы)
```

---

## 🔧 Полезные команды:

```bash
# Логи
pm2 logs vk-ads-scaler

# Перезапуск
pm2 restart vk-ads-scaler

# Статус
pm2 status

# Nginx логи
tail -f /var/log/nginx/access.log
```

---

## 🚨 Важно:

1. **Замените домен** в файлах на свой!
2. **Настройте firewall:**
   ```bash
   ufw allow 22/tcp
   ufw allow 80/tcp
   ufw allow 443/tcp
   ufw enable
   ```
3. **Регулярно обновляйте систему:**
   ```bash
   apt update && apt upgrade -y
   ```

---

## 💰 Стоимость:

- VPS: ~200-300₽/мес
- Домен: ~200₽/год
- SSL: бесплатно (Let's Encrypt)

**Итого: ~300₽/мес**

---

## 🎉 Готово!

После развертывания сервис доступен по адресу:
**https://ваш-домен.ru**

Пользователи просто перетаскивают кнопку в закладки и всё работает! ⚡
